# Personal Instructions

- Always generate React + TypeScript code following strict mode.
- Use functional components and hooks.
- Use Tailwind CSS for styling, with mobile-first responsive patterns.
- Write clean, readable code with descriptive names and minimal comments unless clarity requires them.
- Use accessible UI patterns and include ARIA attributes when appropriate.
